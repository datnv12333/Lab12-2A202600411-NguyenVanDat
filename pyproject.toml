[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
ignore = [
    "E501",   # line too long — handled by formatter
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"
